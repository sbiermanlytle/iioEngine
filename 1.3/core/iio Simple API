EVERY OBJECT
	-canvases
	-objs
	-app (alias for base canvas)
	-b2D Objects

	HAS THESE PROPERTIES:
		pos : Vec
		rotation : float
		z : int
		style : 
			color
			outline
			lineWidth
			lineCap
			imgRepeat : [x,y]
			shadow
				color
				blur
				offset
			dash
				segments
				offset

		OPTIONAL
		hidden : Boolean
		tag : String
		objs : Array of objects
		img : current image
		anims : Array of animations
		animKey : current animation 
		animFrame : current animation frame

	AND THESE FUNCTIONS:
		add(objs)
		rmv(objs)
		color(color)
		outline(color, dash)
		setImage(img,fitted,repeat)
		addAnim(anim)
		setAnim(animKey,animFrame)
		set(iioScript)
		draw(ctx)

EVERY IIO OBJECT
	-canvases
	-objs
	-apps (alias for base canvas)

	HAS THESE PROPERTIES

		OPTIONAL
		center
		width
		height
		radius
		scale
		origin
		R
		C
		res
		bezier
		vel
		acc
		fade
		shrink
		grow
		bounds

	AND THESE FUNCTIONS
		translate( v, y )
		rotate( radiansToRotate )
		right()
		left()
		bottom()
		top()

		OPTIONAL
